class Solution {
public:
    int singleNumber(vector<int>& nums) { 
        //change value with referance
        int ans=0;
        for(int val:nums){
            ans = ans^val;
        }
        return ans;
        
    }
};
